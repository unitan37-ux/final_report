<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title><%= review.title %>の詳細</title>
    <style>
        /* --- 基本設定 --- */
        body {
            background-color: #ffffff;
            color: #333333;
            font-family: "Yu Mincho", "Hiragino Mincho ProN", serif;
            margin: 0;
            padding: 20px;
            text-align: center;
        }

        .container {
            max-width: 700px;
            margin: 0 auto;
            border: 1px solid #cccccc;
            padding: 30px;
            background-color: #ffffff;
        }

        /* --- 作品名 --- */
        h1 {
            color: #333333;
            border-bottom: 2px solid #333333;
            padding-bottom: 10px;
            margin-bottom: 20px;
            font-size: 2em;
        }

        /* --- 表のデザイン --- */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
            font-size: 1.1em;
        }

        th, td {
            border: 1px solid #999999;
            padding: 12px;
            text-align: left;
        }

        th {
            background-color: #f5f5f5;
            width: 30%;
            font-weight: normal;
        }

        td {
            background-color: #ffffff;
        }

        /* --- ボタン類 --- */
        a { color: #0066cc; text-decoration: none; }
        a:hover { text-decoration: underline; }
        
        .btn {
            display: inline-block;
            padding: 10px 20px;
            background: #eeeeee;
            color: #333;
            border: 1px solid #999;
            margin: 5px;
            cursor: pointer;
            font-size: 0.9em;
        }
        .btn:hover { background-color: #dddddd; }
        
        .btn-delete {
            background-color: #fff0f0;
            color: #cc0000;
            border-color: #cc0000;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1><%= review.title %></h1>

        <table>
            <tr>
                <th>作品名</th>
                <td><%= review.title %></td>
            </tr>
            <tr>
                <th>ハード</th>
                <td><%= review.hardware %></td>
            </tr>
            <tr>
                <th>発売年</th>
                <td><%= review.release_year %>年</td>
            </tr>
            <tr>
                <th>点数</th>
                <td><%= review.score %> / 100点</td>
            </tr>
            <tr>
                <th>レビュータイトル</th>
                <td><%= review.review_title %></td>
            </tr>
            <tr>
                <th>詳細コメント</th>
                <td style="white-space: pre-wrap; color: #555;"><%= review.comment %></td>
            </tr>
        </table>

        <div>
            <a href="/reviews/edit/<%= review.id %>" class="btn">編集</a>
            
            <form action="/reviews/delete/<%= review.id %>" method="POST" style="display:inline;" onsubmit="return confirm('本当に削除しますか？');">
                <input type="submit" value="消去" class="btn btn-delete">
            </form>
        </div>

        <p style="margin-top: 20px;">
            <a href="/reviews">一覧に戻る</a>
        </p>
    </div>

</body>
</html>