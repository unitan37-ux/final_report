<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>マップ編集</title>
    <style>
        /* フォーム用のスタイル */
        body { text-align: center; font-family: "Yu Mincho", serif; }
        form {
            max-width: 600px;
            margin: 0 auto;
            text-align: left;
            padding: 20px;
            border: 1px solid #ccc;
        }
        label { display: block; margin-bottom: 10px; font-weight: bold; }
        input[type="text"], input[type="number"], textarea {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
            margin-top: 5px;
        }
        textarea { height: 80px; }
        .submit-btn {
            background-color: #333;
            color: #fff;
            padding: 10px 30px;
            border: none;
            cursor: pointer;
            display: block;
            margin: 20px auto 0;
        }
        .submit-btn:hover { background-color: #555; }
    </style>
</head>
<body>
    <h1><%= map.map_name %> の編集</h1>
    
    <form action="/maps/update/<%= map.id %>" method="POST">
        <label>マップ名: <input type="text" name="map_name" value="<%= map.map_name %>" required></label>
        <label>目標魔力 (G): <input type="number" name="target_g" value="<%= map.target_g %>"></label>
        <label>構造の特徴: <input type="text" name="structure" value="<%= map.structure %>"></label>
        <label>属性傾向: <input type="text" name="element_trend" value="<%= map.element_trend %>"></label>
        <label>おすすめカード: <input type="text" name="recommended_card" value="<%= map.recommended_card %>"></label>
        
        <label>攻略メモ: <textarea name="strategy"><%= map.strategy %></textarea></label>

        <input type="submit" value="更新" class="submit-btn">
    </form>
    
    <p style="margin-top: 20px;">
        <a href="/maps">一覧に戻る</a>
    </p>
</body>
</html>